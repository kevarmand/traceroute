# ft_traceroute

Minimal project skeleton for ft_traceroute.

Current state:
- CLI parsing with mini_argp
- project layout ready
- placeholder modules for sockets, session, probes, packets, output and debug
